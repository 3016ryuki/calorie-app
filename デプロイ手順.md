# PWA デプロイ手順（Vite版）

`calorie-web/` フォルダが対象です。`calorie-app/`（Expo版）はもう使いません。

---

## 1. ローカルで動作確認

```bash
cd calorie-web

# 開発サーバー起動（爆速）
npm run dev
```

→ `http://localhost:5173` がブラウザで開きます。コードを保存すると即座に反映されます。

---

## 2. PWAビルド

```bash
npm run build
```

`dist/` フォルダに本番用ファイルが生成されます（約175KB）。

---

## 3. Vercel に無料デプロイ

### ① Vercelアカウントを作成
https://vercel.com/signup → GitHubなどで登録（無料）

### ② Vercel CLIインストールしてデプロイ

```bash
# 一度だけインストール
npm install -g vercel

# プロジェクトルートでデプロイ（calorie-web/ で実行）
vercel
```

対話形式で：
- `Set up and deploy?` → `Y`
- `Which scope?` → 自分のアカウント
- `Link to existing project?` → `N`
- `What's your project's name?` → `calorie-app`（任意）
- `In which directory is your code located?` → `./`
- `Want to modify these settings?` → `N`（Viteは自動検出されます）

完了すると `https://calorie-app-xxxx.vercel.app` のようなURLが発行されます。

### ③ 本番デプロイ

```bash
vercel --prod
```

---

## 4. iPhone のホーム画面に追加

1. iPhone の **Safari** で発行されたURLを開く
2. 下部の **共有ボタン**（□に↑のアイコン）をタップ
3. **「ホーム画面に追加」** をタップ
4. ホーム画面に「カロリー」アイコンができる

タップすると **フルスクリーンでアプリのように起動**します。

---

## 5. 更新する場合

コードを修正したら：

```bash
npm run build
vercel --prod
```

数秒で更新が反映されます。ホーム画面のアイコンはそのまま使えます。

---

## データについて

- すべて **ブラウザの localStorage** に保存
- Safari のサイトデータを削除すると消えます
- 別の端末では同じデータは見られません（個人利用向け）
- 設定画面の「データをリセット」で全消去可能

---

## トラブルシューティング

| 症状 | 対処 |
|------|------|
| `npm run dev` でエラー | Node v16以上であることを確認 |
| デプロイ後アイコンが反映されない | Safariを完全終了→再起動 |
| データが消えた | 設定→「データをリセット」を実行して再構築 |
